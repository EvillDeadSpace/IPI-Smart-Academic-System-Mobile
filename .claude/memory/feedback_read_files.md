---
name: Read files directly without asking
description: User wants Claude to read files directly instead of asking them to show/paste code
type: feedback
---

Read project files directly using the Read tool instead of asking the user to show or paste code.

**Why:** User finds it frustrating when Claude asks "show me the code" or "paste the file" when Claude can just read it.

**How to apply:** Whenever context requires seeing a file, just read it. Only ask if the path is genuinely unknown.
